#include "include/csv.h"
#include <string.h>
#include <stdlib.h>

Csv *parseCsv(char *csvStr)
{
  Csv *csv = malloc(sizeof(struct CSV_STRUCT));

  return csv;
}

char **parseHeader(char *csvStr)
{
  return (void *)0;
}